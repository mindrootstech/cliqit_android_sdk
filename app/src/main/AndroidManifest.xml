<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.intent.action.VIEW" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application>
        <!-- The SDK does not require any default activities or services.
             Deep link intent-filters should be added to the consumer application's
             activities in their own AndroidManifest.xml -->
    </application>

</manifest>
